# Compiled Go binaries
# The output of the `go build` command.
anti-bot-tool
anti-bot-tool.exe

# Configuration files
# The config.yaml contains sensitive proxy credentials and should not be
# committed to version control. It's recommended to create a
# `config.example.yaml` file to track the required structure.
config.yaml

# Go dependency directory
# Although not used in this project, it's a standard practice to ignore.
vendor/

# Log files
*.log
*.log.*

# IDE and editor-specific files and directories
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# OS-generated files
.DS_Store
Thumbs.db
